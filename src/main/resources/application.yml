server:
  port: 8001

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql_mysql-db_1:3306/glossary?serverTimezone=Asia/Seoul
    username: root
    password: admin

  jpa:
    hibernate:
      #ddl-auto: validate
      ddl-auto: create
    properties:
      hibernate:
        show_sql: false
        format_sql: false


  application:
    name: glossaryapi


logging.level:
  org.hibernate.SQL: info
#  org.hibernate.type: trace
